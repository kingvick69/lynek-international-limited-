# NewsData.io — https://newsdata.io/documentation
# Copy this file to .env (local) and add the same variable in Vercel → Settings → Environment Variables.
# Use the API key from your NewsData.io dashboard (Account → API Key). Redeploy after changing.
NEWSDATA_API_KEY=
